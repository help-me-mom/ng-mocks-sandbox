services:
  core:
    image: satantime/puppeteer-node:24.15.0
    platform: linux/amd64
    working_dir: /app
    volumes:
      - .:/app
      - gyp:/root/.node-gyp
      - npm:/root/.npm
    command:
      - npm
      - install
      - --no-audit
      - --omit=optional

volumes:
  npm:
  gyp:
