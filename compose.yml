services:
  core:
    image: satantime/puppeteer-node:26.1.0
    platform: linux/amd64
    working_dir: /app
    volumes:
      - .:/app
      - gyp:/root/.node-gyp
      - npm:/root/.npm
    command:
      - npm
      - install
      - --no-audit
      - --omit=optional

volumes:
  npm:
  gyp:
